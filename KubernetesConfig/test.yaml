apiVersion: v1
kind: Service
metadata:
  name: agones-gameserver
spec:
  type: LoadBalancer
  selector:
    agones.dev/gameserver: simple-game-server-jxlv8
  ports:
  - protocol: UDP
    port: 7000 # local port
    targetPort: 7160